config:
  target: 'http://localhost:8080'
  phases:
    - duration: 600
      arrivalRate: 100
  ensure:
    p95: 30
    maxErrorRate: 1
scenarios:
  - flow:
    - get:
        url: "/about"
    - get:
        url: "/version"
    - get:
        url: "/info"
